version: '3'

services:
  web:
    build: .
    command: sh -c "uvicorn trading_api:app --host 0.0.0.0 --port 8080"
    ports:
      - "8080:8080"
    depends_on:
      - redis

  redis:
    image: "redis"
    ports:
      - "6379:6379"
